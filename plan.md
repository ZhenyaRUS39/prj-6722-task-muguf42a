# Plan: task-muguf42a

**Project:** `PRJ-4043`  
**Task ID:** `task-muguf42a`  
**Repo:** `prj-6722-task-muguf42a`  
**Progress:** 1/5 subtasks done

## Summary

План: создать структуру репозитория с конфигом, реализовать парсер YAML с валидацией схемы через JSON Schema, написать юнит-тесты и интеграционный тест на CLI. Проект будет на Python с jsonschema/pyyaml.

## Subtasks

### ❌ 1. Инициализировать структуру репозитория

- **ID:** `sub-1`
- **Profile:** `20razrab1`
- **Status:** `failed`
- **Description:** Создать репозиторий prj-6722-task-muguf42a: README.md, requirements.txt (pyyaml, jsonschema, pytest), .gitignore, базовая директория src/yaml_validator/
- **Test plan:** Репозиторий клонируется, pip install -r requirements.txt проходит без ошибок, директории src/yaml_validator/ и tests/ существуют

### ⬜ 2. Реализовать модуль парсера и валидатора

- **ID:** `sub-2`
- **Profile:** `20razrab1`
- **Status:** `pending`
- **Description:** В src/yaml_validator/parser.py реализовать функции load_yaml(path) и validate(data, schema). Парсинг через PyYAML, валидация через jsonschema с понятными ошибками (путь + сообщение). Добавить CLI-обёртку cli.py с аргументами --file и --schema
- **Test plan:** python -m yaml_validator.cli --file tests/fixtures/valid.yaml --schema tests/fixtures/schema.json возвращает код 0; на невалидном файле возвращает код 1 и печатает путь до ошибки
- **Dependencies:** `sub-1`

### ⬜ 3. Написать схему и фикстуры

- **ID:** `sub-3`
- **Profile:** `30razrab2`
- **Status:** `pending`
- **Description:** Создать tests/fixtures/schema.json (пример: обязательные поля name:str, version:semver, optional tags:list) и фикстуры valid.yaml / invalid.yaml (с нарушением типов и отсутствующим полем)
- **Test plan:** Файлы существуют, schema.json валидный JSON, valid.yaml проходит проверку вручную через jsonschema, invalid.yaml — не проходит
- **Dependencies:** `sub-1`

### ⬜ 4. Написать unit-тесты

- **ID:** `sub-4`
- **Profile:** `30razrab2`
- **Status:** `pending`
- **Description:** В tests/test_parser.py покрыть: парсинг валидного YAML, обработка несуществующего файла, обработка битого YAML (IndentationError), успешная валидация, провальная валидация с проверкой сообщения об ошибке
- **Test plan:** pytest tests/ -v показывает все тесты зелёными, покрытие парсера >= 80%
- **Dependencies:** `sub-2`, `sub-3`

### ⬜ 5. Финальная проверка и README

- **ID:** `sub-5`
- **Profile:** `round-robin`
- **Status:** `pending`
- **Description:** README.md с примерами использования CLI и API, команда запуска тестов, badges (по желанию). Прогнать pytest -v и python -m yaml_validator.cli на обоих фикстурах
- **Test plan:** pytest -v зелёный; CLI корректно отрабатывает на valid.yaml (exit 0) и invalid.yaml (exit 1); README содержит секции Installation, Usage, Examples
- **Dependencies:** `sub-4`


---
*Generated by Hermes Orchestrator*
